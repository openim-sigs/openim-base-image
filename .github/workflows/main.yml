name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Run dessant/lock-threads
      run: dessant/lock-threads@v4
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
